# План развития OmniGuard

## Выполненные задачи

- [x] Добавить venv
- [x] Переехать на uv

## Инфраструктура и конфигурация

- [x] `.env` — вынести конфигурацию из кода в переменные окружения. Создан `config.py` (pydantic-settings), `.env`, `.env.example`. Подключено в `connection.py`, `main.py`, `personDetector.py`
- [x] Завернуть PostgreSQL в Docker — `docker-compose.yml` (только БД, т.к. приложение на PySide6 запускается нативно)
- [x] `.gitignore` — добавлен
- [x] Добавить линтер/форматтер (Ruff) — настроен в `pyproject.toml`
- [ ] Настроить миграции Alembic для версионирования схемы БД. Сейчас таблицы создаются вручную через `CREATE TABLE IF NOT EXISTS` в `connection.py`. После перехода на ORM (SQLAlchemy) — подключить Alembic для автогенерации миграций

## Архитектура и рефакторинг

- [ ] Написать ORM для доступа к БД (а не напрямую, как сейчас). Текущая реализация в `connection.py` использует raw SQL через `QSqlQuery` с ручной конкатенацией строк
- [ ] Провести рефакторинг текущего кода:
  - [ ] **Дублирование**: В `main.py` метод `open_settings_window` определён дважды (строки ~378 и ~416), второй переопределяет первый
  - [ ] **Naming conventions (PEP 8)**: Привести именование к единому стилю — `Email_server` → `EmailServer`, `personDetector.py` → `person_detector.py`, `reciever` → `receiver` (опечатка во всём проекте)
  - [ ] **Разделение ответственности**: `main.py` (513 строк) содержит и GUI, и VideoThread, и бизнес-логику. Разнести по отдельным модулям
  - [ ] **Удалить неиспользуемую зависимость**: `psycopg2-binary` указан в `pyproject.toml`, но нигде не используется (используется Qt SQL driver QPSQL)
- [ ] Переехать на слоистую архитектуру (когда-то в будущем)

## Модель и детекция

- [ ] Поменять модель на модель из семейства трансформеров (вероятно на RF-DETR)
- [ ] Добавить счётчик FPS (необходимо, чтобы работа отвечала теме ВКР)
- [ ] Добавить возможность выбора порога уверенности (confidence threshold) через GUI. Сейчас захардкожен `0.4` в `personDetector.py`
- [ ] Добавить поддержку GPU ускорения (CUDA) с автоматическим fallback на CPU

## Безопасность

- [ ] **SQL-инъекции**: В `connection.py` методы `add_cam_exec` (строка ~119) и `add_zone_exec` (строка ~160) используют f-string для формирования SQL-запросов вместо параметризованных запросов. Это критическая уязвимость
- [ ] **Хранение паролей**: Пароль БД хранится в открытом виде в коде, email-пароль вводится и передаётся через GUI без шифрования

## Обработка ошибок и стабильность

- [ ] В `camera.py` метод `read()` может вернуть `None` при ошибке чтения кадра — бесконечный цикл `while not self.cap.isOpened()` без таймаута и без обработки случая когда `cap.read()` возвращает `(False, None)`
- [ ] Заменить `print()` на модуль `logging` для структурированного логирования с уровнями (DEBUG, INFO, WARNING, ERROR)
- [ ] В `email_server.py` SMTP-соединение создаётся в конструкторе один раз и не переподключается при разрыве
- [ ] Блокирующий `server_socket.accept()` в `VideoThread.run()` — при отсутствии клиента поток зависает

## UI/UX улучшения

- [ ] Переработка интерфейса
- [ ] Возможность задавать зону через прямоугольник (упрощенный режим)
- [ ] Добавление нескольких зон — *уже реализовано в `zone_redactor.py`*, но требует доработки
- [ ] Добавление зоны-исключения (в которой не нужно распознавать)
- [ ] Отображение FPS и статуса детектора в главном окне
- [ ] Предпросмотр всех камер одновременно (grid view)

## Запись и хранение видео

- [ ] БД для метаданных видеозаписей (время начала/конца, камера, количество детекций)
- [ ] Каждое видео фиксированной длины (например, 1 час)
- [ ] На видео/в метаданных должны быть метки, в какой промежуток времени был обнаружен человек (как в кадре, так и в зоне)
- [ ] Воспроизведение записанных видео из приложения

## Настройки

- [ ] Указание почты куда будут приходить уведомления — *частично реализовано*
- [ ] Изменение качества видео
- [ ] Настройка частоты детекции (N-й кадр, сейчас захардкожено = 5)
- [ ] Настройка времени жизни видеозаписей (сейчас захардкожено 48 часов в `main.py`)

## Тестирование

- [ ] Написать unit-тесты (хотя бы для `connection.py`, `personDetector.py`, `out_of_date_video_cleaner.py`)
- [ ] Добавить CI/CD пайплайн (GitHub Actions) для запуска линтера и тестов

## Идеи

- ComboBox для выбора камеры — *уже реализовано*
- Сохранение изображения в зоне в более высоком качестве (фон в 360p, зона в 480p отдельно)
- Поддержка нескольких камер (хотя бы 2) — *уже реализовано*
